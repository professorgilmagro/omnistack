{
	"name": "omnistack",
	"version": "1.0.0",
	"private": true,
	"main": "./backend/index.js",
	"license": "MIT",
	"scripts": {
		"start": "yarn --cwd frontend start",
		"heroku-prebuild": "yarn --cwd backend install && yarn --cwd frontend install",
		"heroku-postbuild": "yarn --cwd backend start"
	},
	"engines": {
		"node": ">=10.15.0",
		"yarn": ">=1.16"
	}
}
