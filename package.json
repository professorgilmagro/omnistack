{
	"name": "omnistack",
	"version": "1.0.0",
	"private": true,
	"main": "./backend/index.js",
	"license": "MIT",
	"scripts": {
		"start": "yarn --cwd backend start",
		"heroku-prebuild": "yarn --cwd backend install",
		"heroku-postbuild": "yarn --cwd frontend install"
	},
	"engines": {
		"node": ">=10.15.0",
		"yarn": ">=1.16"
	}
}
